package com.prism.prism_catalog.model.enums;

public enum VideoVisibility {
    PUBLIC,       // visible in public catalog
    PRIVATE,      // only owner app can access
    UNLISTED      // not listed publicly, but accessible via direct link
}
